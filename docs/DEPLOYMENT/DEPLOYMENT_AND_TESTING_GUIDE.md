# AutoDiary v3.0 完整部署和测试指南

## 概述

本指南详细说明了 AutoDiary v3.0 固件的完整部署和测试流程，包括固件烧录、埋点收集、性能分析和实时监控。

## 系统架构

```
┌─────────────────────────────────────────────┐
│        AutoDiary v3.0 智能日记系统           │
├─────────────────────────────────────────────┤
│  固件层 (C++)                                │
│  - 拍摄 (Camera Capture)                     │
│  - 存储 (SPIFFS Storage)                     │
│  - 上传 (HTTP Upload)                        │
│  - 埋点 (Checkpoint Logging)                 │
├─────────────────────────────────────────────┤
│  服务层 (Python)                            │
│  - 部署工具 (Deploy Firmware)                │
│  - 数据采集 (Collect Checkpoints)            │
│  - 实时监控 (Realtime Monitor)               │
│  - 故障诊断 (Fault Diagnostics)              │
├─────────────────────────────────────────────┤
│  设备层                                     │
│  - XIAO ESP32-S3                            │
│  - OV2640 摄像头                             │
│  - SPIFFS 文件系统                           │
└─────────────────────────────────────────────┘
```

## 快速开始

### 前置条件

1. **硬件**
   - XIAO ESP32-S3 开发板
   - OV2640 摄像头模块
   - USB 数据线

2. **软件环境**
   - Python 3.8+
   - PlatformIO CLI
   - 必要的 Python 包：requests

3. **网络配置**
   - 确保设备和电脑在同一网络
   - 修改 WiFi 凭证（如需要）

### 安装依赖

```bash
# 安装 Python 依赖
pip install -r requirements.txt

# 确保 PlatformIO 已安装
platformio --version
```

## 部署流程

### 步骤 1: 准备固件

固件源文件：`src/main_with_checkpoints.cpp`

主要特性：
- ✅ 完整的埋点实现（18 个检查点）
- ✅ 内存和性能监控
- ✅ HTTP 服务器接口
- ✅ JSON 格式的埋点导出

### 步骤 2: 编译和烧录

#### 方法 A：使用自动化脚本（推荐）

```bash
# 只编译（不烧录）
python deploy_firmware.py --skip-upload

# 编译并烧录
python deploy_firmware.py

# 编译、烧录并监控
python deploy_firmware.py --monitor --duration 30
```

#### 方法 B：手动使用 PlatformIO

```bash
# 检测设备
platformio device list

# 编译固件
platformio run -e seeed_xiao_esp32s3

# 烧录到指定端口
platformio run -e seeed_xiao_esp32s3 -t upload --upload-port COM3

# 监控串口输出
platformio device monitor --port COM3
```

### 步骤 3: 验证固件

烧录成功后，设备应该：
1. 连接到配置的 WiFi 网络
2. 启动 HTTP 服务器（端口 80）
3. 初始化摄像头和存储系统

验证方法：

```bash
# 检查设备状态
curl http://192.168.1.11/status

# 响应示例
{
  "device": "XIAO-ESP32S3-Sense",
  "mode": "Full Cycle with Checkpoints",
  "version": "v3.0",
  "checkpoints_logged": 0,
  "last_phase": 0
}
```

## 埋点数据收集

### 完整周期测试

```bash
# 运行 3 个完整周期的测试
python checkpoint_collector.py --ip 192.168.1.11 --cycles 3

# 带自定义参数
python checkpoint_collector.py \
  --ip 192.168.1.11 \
  --port 80 \
  --cycles 5
```

### 数据收集流程

1. **连接设备** - 验证设备在线
2. **触发周期** - 发送 `/fullcycle` 请求
3. **等待执行** - 设备执行完整的拍摄-存储-上传-清理流程
4. **收集数据** - 从 `/checkpoints` 端点获取埋点数据
5. **分析数据** - 计算性能指标
6. **保存数据** - 存储为 JSON 和 CSV 格式

### 埋点数据结构

```json
{
  "timestamp": "2025-11-30T02:15:00",
  "device": "192.168.1.11:80",
  "checkpoints": [
    {
      "id": 1,
      "phase": 1,
      "elapsed_ms": 125,
      "heap_free": 245000,
      "message": "Photo capture started"
    },
    {
      "id": 2,
      "phase": 1,
      "elapsed_ms": 250,
      "heap_free": 240000,
      "message": "Camera frame get success",
      "frame_size": 50000
    }
  ]
}
```

### 埋点阶段定义

| 阶段 | 名称 | 描述 |
|------|------|------|
| 0 | IDLE | 空闲状态 |
| 1 | CAPTURING | 拍摄中 |
| 2 | STORING | 存储中 |
| 3 | UPLOADING | 上传中 |
| 4 | CLEANUP | 清理中 |
| 5 | COMPLETE | 完成 |
| 6 | ERROR | 错误 |

## 性能分析

### 启动分析工具

数据会自动在收集时分析。关键指标：

```
总埋点数: 18
总耗时: 2500 ms

内存分析:
  初始堆: 500000 bytes
  最终堆: 480000 bytes
  变化: -20000 bytes

阶段分析:
  阶段 1: 平均耗时 250 ms
  阶段 2: 平均耗时 150 ms
  阶段 3: 平均耗时 1800 ms
  阶段 4: 平均耗时 50 ms
```

### 性能优化建议

基于分析结果，可以调整以下参数：

**在 `config.json` 中：**

```json
{
  "camera": {
    "auto_capture_interval": 30,
    "image_quality": 95
  },
  "upload": {
    "chunk_size": 8192,
    "timeout": 30
  }
}
```

**在固件中：**

```cpp
// 调整 JPEG 质量（10-50，值越小文件越小）
config.jpeg_quality = 12;

// 调整帧缓冲区大小
config.fb_count = 1;  // 单缓冲
```

## 实时监控

### 启动监控系统

```bash
# 运行 60 秒的监控
python realtime_monitor.py --ip 192.168.1.11 --duration 60

# 自定义检查间隔
python realtime_monitor.py --interval 3 --duration 120
```

### 监控指标

实时监控系统会收集和显示：

- **连接状态** - 设备在线/离线
- **内存使用** - 堆内存使用率和可用内存
- **埋点统计** - 已记录的检查点数量
- **性能告警** - 内存使用率过高时警告

### 告警阈值

| 事件 | 阈值 | 级别 |
|------|------|------|
| 设备离线 | 连接超时 | CRITICAL |
| 内存使用 > 90% | 512KB 总内存 | CRITICAL |
| 内存使用 > 75% | 512KB 总内存 | WARNING |

## 故障诊断

### 运行诊断工具

```bash
# 分析已收集的埋点数据
python fault_diagnostics.py data/checkpoints/checkpoints_cycle1_121500.json
```

### 诊断项目

诊断工具会自动检查：

1. **内存泄漏检测**
   - 监控堆内存趋势
   - 检查是否存在持续的内存下降
   - 识别未释放的缓冲区

2. **性能瓶颈诊断**
   - 找出耗时最长的阶段
   - 建议优化方向

3. **堆碎片化诊断**
   - 分析内存波动
   - 检测碎片化迹象

4. **网络问题诊断**
   - 检查上传耗时
   - 评估网络性能

5. **摄像头诊断**
   - 验证摄像头初始化
   - 检查帧大小

6. **存储诊断**
   - 验证 SPIFFS 功能
   - 检查文件保存性能

### 诊断报告示例

```
诊断报告
=================================================

检查点文件: data/checkpoints/checkpoints_cycle1.json
总检查点数: 18

检测到 2 个潜在问题:

❌ 性能瓶颈诊断
   - 阶段 3 耗时过长: 1800ms

⚠️  内存泄漏检测
   - 检测到可能的内存泄漏: 内存呈下降趋势，总减少 20.0KB

优化建议:
1. 优化阶段 3 的处理逻辑
2. 检查是否存在未释放的缓冲区或文件句柄
3. 检查 esp_camera_fb_return() 是否被正确调用
=================================================
```

## 完整测试工作流示例

### 场景：完整的部署和测试

```bash
#!/bin/bash

# 1. 编译和烧录固件
echo "=== 步骤 1: 部署固件 ==="
python deploy_firmware.py --monitor --duration 20

# 等待设备启动
echo "=== 等待设备启动... ==="
sleep 10

# 2. 收集埋点数据
echo "=== 步骤 2: 收集埋点数据 ==="
python checkpoint_collector.py --ip 192.168.1.11 --cycles 3

# 3. 启动实时监控
echo "=== 步骤 3: 启动实时监控 ==="
python realtime_monitor.py --ip 192.168.1.11 --duration 60 &

# 4. 触发数据收集
echo "=== 步骤 4: 触发完整周期 ==="
curl http://192.168.1.11/fullcycle

# 5. 等待和分析
echo "=== 步骤 5: 分析埋点数据 ==="
sleep 15
python fault_diagnostics.py data/checkpoints/checkpoints_cycle1_*.json

# 6. 生成最终报告
echo "=== 部署和测试完成 ==="
```

## 文件结构

```
AutoDiary/
├── src/
│   ├── main_with_checkpoints.cpp  # v3.0 完整埋点固件
│   └── main.cpp                   # 原始固件
├── deploy_firmware.py             # 固件部署工具
├── checkpoint_collector.py         # 埋点收集工具
├── realtime_monitor.py            # 实时监控系统
├── fault_diagnostics.py           # 故障诊断工具
├── data/
│   ├── checkpoints/               # 埋点数据目录
│   ├── monitoring/                # 监控日志目录
│   └── diagnostics/               # 诊断报告目录
├── config.json                    # 系统配置
├── platformio.ini                 # PlatformIO 配置
└── requirements.txt               # Python 依赖
```

## 故障排除

### 问题 1：设备无法连接到 WiFi

**症状**：固件启动后无法连接到指定的 WiFi 网络

**解决方案**：
1. 检查 WiFi 凭证是否正确（在固件中修改）
2. 确保 WiFi 信号强度充足
3. 重启设备和路由器

### 问题 2：摄像头初始化失败

**症状**：串口输出 "❌ Camera init failed"

**解决方案**：
1. 检查硬件连接
2. 验证引脚配置（`include/camera_pins.h`）
3. 确保 PSRAM 可用

### 问题 3：上传失败

**症状**：埋点中显示 "Upload failed"

**解决方案**：
1. 检查服务器地址配置
2. 验证网络连接
3. 检查服务器是否运行
4. 查看防火墙规则

### 问题 4：内存不足

**症状**：频繁的内存泄漏警告

**解决方案**：
1. 减小图像质量
2. 减小缓冲区大小
3. 增加运行间隔
4. 检查是否有无限循环

## 性能基准

基于 XIAO ESP32-S3 硬件的典型性能指标：

| 操作 | 耗时 | 内存 |
|------|------|------|
| 拍摄 | 200-300 ms | 0-5 MB |
| 存储 | 100-200 ms | ~文件大小 |
| 上传 | 1-3 秒 | 文件缓冲 |
| 完整周期 | 2-4 秒 | 恢复到初始值 |

## 配置优化

### 低功耗模式

```cpp
// 在 main_with_checkpoints.cpp 中调整
config.jpeg_quality = 20;        // 降低质量，减小文件
config.frame_size = FRAMESIZE_VGA; // 保持 VGA 分辨率
```

### 高性能模式

```cpp
config.jpeg_quality = 10;        // 提高质量
config.frame_size = FRAMESIZE_XGA; // 提高分辨率
```

## 高级主题

### 添加自定义埋点

在固件中添加新的埋点：

```cpp
// 在相关位置调用
logCheckpoint(20, PHASE_CUSTOM, "Custom operation started", 0);

// 执行操作...

logCheckpoint(21, PHASE_CUSTOM, "Custom operation completed", 0);
```

### 扩展诊断功能

修改 `fault_diagnostics.py` 以添加自定义检查：

```python
def diagnose_custom(self) -> Tuple[FaultLevel, List[str]]:
    # 实现自定义诊断逻辑
    return FaultLevel.OK, []
```

### 集成到 CI/CD

在 GitHub Actions 或 GitLab CI 中集成测试：

```yaml
# .github/workflows/test.yml
- name: Build and Test
  run: |
    python deploy_firmware.py --skip-upload
    python checkpoint_collector.py --cycles 1
```

## 支持和反馈

- 📧 问题报告：提交 Issue
- 💬 讨论：开启 Discussion
- 📚 文档：查看 README.md

## 更新日志

### v3.0 (当前版本)
- ✅ 添加完整埋点系统
- ✅ 实现实时监控
- ✅ 添加故障诊断
- ✅ 优化内存管理

### v2.0
- 网络功能优化
- 存储系统改进

### v1.0
- 初始版本发布

---

**最后更新**：2025-11-30
**版本**：v3.0
**状态**：生产就绪 ✅
