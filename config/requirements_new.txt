# AutoDiary 增强版依赖包
# 包含集成FunASR、摄像头Web管理、智能分析等功能的依赖

# 基础依赖
asyncio-mqtt>=0.13.0
websockets>=11.0.3
aiofiles>=23.2.1
python-json-logger>=2.0.7

# Web服务器依赖
aiohttp>=3.9.0
aiohttp-cors>=0.7.0

# 图像处理
Pillow>=10.1.0
opencv-python>=4.8.0

# 音频处理
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0

# FunASR 语音识别框架
funasr>=1.0.0
modelscope>=1.10.0
huggingface-hub>=0.19.0

# 机器学习和数据处理
torch>=2.0.0
torchaudio>=2.0.0
scikit-learn>=1.3.0

# 数据处理和存储
pandas>=2.1.0
pyarrow>=13.0.0
sqlite3

# 文本处理和NLP
jieba>=0.42.1
textblob>=0.17.1

# 配置和日志
pyyaml>=6.0.1
python-dotenv>=1.0.0
colorlog>=6.7.0

# 系统监控和性能
psutil>=5.9.0
memory-profiler>=0.61.0

# 网络和HTTP
requests>=2.31.0
urllib3>=2.0.0
certifi>=2023.7.0

# 时间和日期处理
python-dateutil>=2.8.2
pytz>=2023.3

# 数据验证和序列化
pydantic>=2.4.0
marshmallow>=3.20.0

# 缓存和存储
redis>=5.0.0
diskcache>=5.6.0

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# 开发工具
black>=23.9.0
flake8>=6.1.0
mypy>=1.6.0

# 可选：GPU加速支持
# torch-audio>=2.0.0+cu118  # 根据CUDA版本选择
# nvidia-ml-py>=12.535.0

# 可选：Docker支持
docker>=6.1.0

# 可选：数据库ORM
sqlalchemy>=2.0.0
alembic>=1.12.0

# 可选：异步任务队列
celery>=5.3.0
kombu>=5.3.0

# 可选：Web界面增强
jinja2>=3.1.0
click>=8.1.0

# 可选：API文档
fastapi>=0.104.0
uvicorn>=0.24.0

# 可选：数据可视化
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0

# 可选：导出功能
openpyxl>=3.1.0
xlsxwriter>=3.1.0
reportlab>=4.0.0

# 可选：安全加密
cryptography>=41.0.0
bcrypt>=4.0.0

# 可选：进度条和用户界面
tqdm>=4.66.0
rich>=13.6.0

# 可选：配置管理
configparser>=6.0.0
environs>=9.5.0

# 可选：日志轮转
loguru>=0.7.0

# 可选：网络发现
zeroconf>=0.71.0

# 可选：串口通信（用于设备管理）
pyserial>=3.5

# 可选：文件监视
watchdog>=3.0.0

# 版本锁定（确保兼容性）
# 核心依赖版本
# asyncio>=3.11.0
# websockets==11.0.3
# aiohttp==3.9.0
# funasr==1.0.0

# 系统要求说明：
# Python >= 3.8
# 推荐使用 Python 3.9+ 以获得最佳性能
# Windows: 需要 Visual C++ Redistributable
# Linux: 需要安装 libsndfile1, ffmpeg
# macOS: 需要安装 Homebrew 和相关依赖

# 安装命令：
# pip install -r requirements_new.txt

# FunASR 额外安装：
# pip install -U funasr
# pip install -U modelscope

# GPU支持（可选）：
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
